# Кристина 6.0 — Зависимости (обновлено 2026)

# ── Ядро ──
ollama>=0.5.0               # Ollama Python client (tool calling + format="json")
pydantic>=2.10.0            # Типизированная конфигурация
pydantic-settings>=2.4.0    # .env + env vars


# ── Производительность ──
orjson>=3.10.0              # Быстрая JSON сериализация (5×)
aiofiles>=24.1.0            # Async файловые операции
numpy>=2.0.0                # NumPy 2 — для векторных операций
httpx[http2]>=0.28.0        # Async HTTP клиент (замена requests)

# ── Память и поиск ──
chromadb>=0.5.0             # Векторная БД (PersistentClient)
networkx>=3.3               # Knowledge Graph

# ── Системные ──
psutil>=6.0.0               # Мониторинг системы
watchdog>=4.0.0             # Наблюдение за файлами
schedule>=1.2.0             # Планировщик

# ── HTTP ──
requests>=2.32.0            # HTTP клиент (legacy, для bs4)
beautifulsoup4>=4.12.0      # HTML парсинг

# ── Поиск ──
duckduckgo-search>=6.0.0    # DuckDuckGo API

# ── Файловые операции ──
send2trash>=1.8.0           # Безопасное удаление в корзину

# ── Конфигурация ──
python-dotenv>=1.0.0        # .env файлы
pyyaml>=6.0                 # YAML конфигурация личности

# ── Rust ядро (сборка) ──
maturin>=1.7.0              # Сборка Rust → Python wheel

# ══════════════════════════════════════════
# Опциональные
# ══════════════════════════════════════════

# tiktoken>=0.8.0           # Точный подсчёт токенов
# flashrank>=0.2.0          # Reranking для RAG
# faster-whisper>=1.0.0     # STT — голосовой ввод
# piper-tts>=1.2.0          # TTS — голосовой вывод
# pynvml>=12.0.0            # NVIDIA GPU мониторинг
# selectolax>=0.3.0         # Быстрый HTML парсер (замена bs4, 20×)
# cachetools>=5.0.0         # TTL/LRU кэш
